[defaults]
# Ansible configuration file for TravelMemory project

# Inventory file location
inventory = ./inventory.ini

# Remote user for SSH connections
remote_user = ec2-user

# SSH key location
private_key_file = ./durga-windows.pem

# Disable host key checking for first-time connections
host_key_checking = False

# Connection timeout (in seconds)
timeout = 30

# Number of parallel processes
forks = 5

# Gather facts
gathering = smart

# Cache facts locally for performance
fact_caching = jsonfile
fact_caching_connection = /tmp/ansible_facts
fact_caching_timeout = 86400

# Logging
log_path = ./ansible.log

# Callback plugins
callback_whitelist = human_log

# Roles path (relative to playbook directory)
roles_path = ../roles:./roles

# Variable precedence
hash_behaviour = replace

# Task name display length
display_skipped_hosts = True

[ssh_connection]
# SSH connection options
pipelining = True
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o StrictHostKeyChecking=no
control_path = /tmp/ansible-ssh-%%h-%%p-%%r
